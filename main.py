from functions import get_input
from p1 import BFS
from p2 import IDS
import copy


if __name__ == "__main__":

    initial_state = get_input()

    # BFS(initial_state)
    IDS(initial_state)
    # A_STAR(initial_state)